package com.example.demo.service.interfaces;

import com.example.demo.entity.Result;

public interface ResultService {
    void saveResult(Result result);
    //boolean checkExists(Long neighborhoodId, Long productId);  
}